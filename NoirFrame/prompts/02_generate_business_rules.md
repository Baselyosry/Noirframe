# Generate BUSINESS_RULES.md

Own `BR-*` only. Derive behavior from confirmed `RQ-*` and `FEAT-*` records. Do not create permissions, state transitions, validations, or lifecycles without evidence. Link applicable rules to sources and record gaps as uncertainty.
